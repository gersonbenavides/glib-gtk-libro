/* BEGIN REFERENCES OOP */

let refs_oop_semi = document.getElementsByClassName("refs-oop-semi");

if( refs_oop_semi.length > 0 ) {

    const chpt_oop_semi = 3;
    let i_oop_semi = 0;
}

/* END REFERENCES OOP */
